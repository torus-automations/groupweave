{
  "name": "@groupweave/near",
  "version": "0.1.0",
  "description": "NEAR blockchain utilities and services for GroupWeave platform",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "lint": "pnpm clean && eslint ."
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@near-js/accounts": "^2.0.0",
    "@near-js/keystores-node": "^2.2.6",
    "@near-js/providers": "^2.0.0",
    "@near-js/signers": "^2.0.0"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "eslint": "^9.31.0",
    "tsup": "^8.0.0",
    "typescript": "^5.1.3"
  },
  "publishConfig": {
    "access": "restricted"
  },
  "private": true
}